
server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
#  mvc:
#    view:
#      prefix: /WEB-INF/views/
#      suffix: .jsp
#  mustache:
#    suffix: .html
#    expose-session-attributes: true  # session처리 및 ui에서 머스테치 사용하기 위한 설정
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 591973622327-g7jugjudrpidfpeg15ls6bucj9ujfuf2.apps.googleusercontent.com
            client-secret: bWcwngZZEyC-zsm1WCZCrFqx
            scope:  # email과 profile만 받아 오겠다.
              - email
              - profile

          facebook:
            client-id: 184494353207222
            client-secret: a56d23db48b45ba2645d23b003fb4460
            scope:
              - email
              - public_profile

          naver:
            client-id: fCHnhxb3MLolrXsxCGGQ
            client-secret: IywLuOew5K
            scope:
              - id
              - email
              - name
            authorization-grant-type: authorization_code
            redirect-uri: http://localhost:8080/login/oauth2/code/naver
            client-name: Naver

          kakao:
            client-id: 9902296fa9f098f659b7e7c44e7dfa15
            client-secret: TVM523yqMiG8aGb4oQQ9uHEleBGfq8oa
            scope:
              - account_email
              - profile
            authorization-grant-type: authorization_code
            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
            client-name: Kakao
            client-authentication-method: POST

        provider:
          naver:
            authorization-uri:  https://nid.naver.com/oauth2.0/authorize  # 이 주소로 요청하면 네이버 로그인 창이 뜬다.
            token-uri:  https://nid.naver.com/oauth2.0/token  # 이 주소로 요청하면 토큰을 받는다.
            user-info-uri:  https://openapi.naver.com/v1/nid/me
            user-name-attribute: response # 회원정보를 json으로 받는데 response라는 키값으로 네이버가 리턴해줌

          kakao:
            authorization-uri:  https://kauth.kakao.com/oauth/authorize
            token-uri:  https://kauth.kakao.com/oauth/token
            user-info-uri:  https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/security1?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: user02
    password: user02

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update  # create, update, run 등 설정
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl # entity파일의 변수명이 그대로 컬럼명이 된다는 의미
      use-new-id-generator-mappings: false  # JPA의 기본 넘버링을 따라가지 않는다, true: 따른다.
    show-sql: false  # 톰캣 부팅 시 sql 실행을 콘솔에 보여줌
    properties:
      hibernate.format_sql: true  # 부팅 시 sql 실행을 보기좋게 콘솔에 보여줌

  jackson:
    serialization:
      fail-on-empty-beans: false

# 카카오 로그인 시  사용
kye:
  key: kye1234
